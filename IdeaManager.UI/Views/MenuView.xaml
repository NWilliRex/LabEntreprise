<UserControl x:Class="IdeaManager.UI.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IdeaManager.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" Margin="10" Background="#1E1E1E">
        <Button Content="Liste des idées" 
                Command="{Binding NavigateToIdeaListCommand}"
                Margin="0,0,10,0"
                Padding="10,5"
                Background="DarkBlue"
                Foreground="White"
                BorderBrush="#005A9E"/>
        <Button Content="Nouvelle idée" 
                Command="{Binding NavigateToIdeaFormCommand}"
                Padding="10,5"
                Background="DarkBlue"
                Foreground="White"
                BorderBrush="#005A9E"/>
    </StackPanel>
</UserControl>
